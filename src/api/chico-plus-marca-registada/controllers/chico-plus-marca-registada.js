'use strict';

/**
 * chico-plus-marca-registada controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::chico-plus-marca-registada.chico-plus-marca-registada');
