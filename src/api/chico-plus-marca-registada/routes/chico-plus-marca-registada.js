'use strict';

/**
 * chico-plus-marca-registada router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::chico-plus-marca-registada.chico-plus-marca-registada');
