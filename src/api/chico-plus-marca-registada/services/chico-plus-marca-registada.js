'use strict';

/**
 * chico-plus-marca-registada service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::chico-plus-marca-registada.chico-plus-marca-registada');
