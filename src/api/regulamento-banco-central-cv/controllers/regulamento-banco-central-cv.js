'use strict';

/**
 * regulamento-banco-central-cv controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::regulamento-banco-central-cv.regulamento-banco-central-cv');
